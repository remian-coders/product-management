{
	"name": "root",
	"private": true,
	"version": "1.0.0",
	"scripts": {
		"dev": "lerna run dev --stream --no-bail",
		"prod": "lerna run prod --stream --no-bail"
	},
	"devDependencies": {
		"lerna": "^5.1.8",
		"nx": "^9999.0.1"
	}
}
