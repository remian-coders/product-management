{
	"name": "root",
	"private": true,
	"version": "1.0.0",
	"scripts": {
		"dev": "lerna run dev --stream --no-bail",
		"prod": "lerna run prod --stream --no-bail",
		"build": "lerna run build"
	},
	"devDependencies": {
		"@types/cors": "^2.8.12",
		"@types/multer": "^1.4.7",
		"lerna": "^5.1.8",
		"nx": "^9999.0.1"
	},
	"dependencies": {
		"cors": "^2.8.5"
	}
}
